## Numeric operations/ random numbers

# Numeric operations
2+2
log(10)
c(1,1)+c(1,1)

# create 10 random numbers with mean 0 and st 1
x <- rnorm(10,mean=0,sd=1)
x                   # random numbers
x <- data.frame(x)  # data frame 
x <- matrix(x)      # 1 list

