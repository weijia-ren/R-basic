## Set the library path - because the default folder has too limited space 
.libPaths(c(.libPaths(),"c:/3.5"))

## R packages: combination of help documents, datasets, functions


# install the package - 1x per computer
install.packages("tidyverse")
# load the package - 1x per R session
library(tidyverse)
# tidyverse include: ggplot2, dplyr, tidyr, readr, purrr, tibble, hms, stringr, lubridate, forcats,
#                   DBI, haven, httr, jsonlite, readxl, rvest, xml2, modelr

